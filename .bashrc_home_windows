
# Machine specific configuration
if [ `hostname` == "ironman" ]; then

    export d="/mnt/c/Users/Joey/Documents/dev"
    inputrcfile="$d/config/.inputrc"
    # alias p='ssh -l joey ubuntu'

    # terminal prompt colour = cyan
    terminalPromptColour=6;
#
# elif [ `hostname` == "ubuntu" ]; then
#
#     export d="/home/joey/dev"
#     export du="/home/joey/devubuntu"
#     inputrcfile="$d/config/.inputrc";
#     export JAVA_HOME="/usr/lib/jvm/java-8-openjdk-amd64";
#     ANDROID_HOME="~/android-sdk-linux";
#     PATH="$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools";
#     export PYTHONPATH="$PYTHONPATH:$d/openai/gym"
#
#     # terminal prompt colour = green
#     terminalPromptColour=2;
fi

if [ -f $inputrcfile ]; then

    export INPUTRC=$inputrcfile

    # Read the inputrc file
    bind -f $INPUTRC;
fi


# Terminal prompt
export PS1="\[$(tput setaf $terminalPromptColour)\]\@ \u@\h:\w>\[$(tput sgr0)\] 
"

# Aliasing common commands to prevent screw ups
#alias rm='trash-put'
alias l='ls -ahl --color --group-directories-first'
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'

# Expand variables with tab
shopt -s direxpand

# Set the size of core files
ulimit -c unlimited

# alias git='/mnt/c/Program\ Files/Git/cmd/git.exe'
